export default function SiteLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <>
      {/* Navigation and Footer will be added here during migration */}
      {children}
    </>
  )
}